#https://www.baeldung.com/linux/docker-container-gui-applications
services:
  playwright:
    build: PlaywrightDockerFile
    container_name: playwright
    # environment:
        # - DISPLAY=${DISPLAY}
    network_mode: host
    # privileged: true
    volumes:
      - ./mesModules:/Playwright/mesModules
      - ./tests:/Playwright/tests
    tty: true
    command: "/bin/bash"
